<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.movie">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".HomeActivity" />

        <activity android:name=".Preview_A1Activity"></activity>
        <activity android:name=".TheaterChoiceActivity" />
        <activity android:name=".CityChoiceActivity" />
        <activity android:name=".SeatChoiceActivity" />
        <!-- <activity android:name=".LoginActivity" /> -->
        <activity android:name=".AreaChoiceActivity" />
        <activity android:name=".DateChoiceActivity" />
        <activity android:name=".MovieDetailedScreenActivity" />
        <activity android:name=".MyInformationActivity" />
        <activity android:name=".MovieChoiceActivity" />
        <!--<activity android:name=".HomeActivity" />-->
        <!--<activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />-->
    </application>

</manifest>